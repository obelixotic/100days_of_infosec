read(1, var_58, 0x40) var_5c = 0 eax = var_5c 
for(i=0; i<=0x2f; i++):
	eax = i
	eax = 1byte{[eax+0x601080]}
	eax = not eax
	edx = eax
	eax = i
	eax = [var_5b + rax]
	if(edx == eax):
		//jmp left
		//var_5c++
	else:
		print "Nope!" //fail condition
print "Correct!" //success condition
	
0x601080
byte	hex val		not in dec	char value	
1	99		102		f
2	93		108		l
3	9e		97		a
4	98			
.
.
.
.

use binary_not.py to convert these and get the flag!
